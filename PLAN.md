# 외부 강의 시간표 자동 편성기 구현 계획

## 1. 목표

초등학교의 전담 시간표를 엑셀 또는 한글 표에서 복사해 웹앱에 붙여넣으면, 각 셀에서 학년·반·과목을 추출해 반별 배정 불가능 시간을 계산하고 외부 강의를 남은 시간에 자동 배정한다.

첫 버전은 서버 없이 브라우저에서만 동작한다. 사용자가 입력하는 강의 조건은 다음 항목으로 제한한다.

- 각 반이 들어야 하는 강의 시간: 1~4교시
- 한 타임에 동시에 수업하는 반 수(선택한 수만큼 함께 배정하고, 나머지 반은 한 타임에 따로 배정)
- 월~금 요일별 외부강의 가능 교시(개별 선택)

2교시 강의는 `1~2교시`, `3~4교시`, `5~6교시` 블록에만 배정한다. 3~4교시 강의는 연속 교시로 배정하며, 허용 블록 생성 규칙은 추후 쉽게 변경할 수 있도록 별도 모듈로 둔다.

## 2. 핵심 사용자 흐름

1. 왼쪽 `저장 목록`을 연다.
2. 저장된 목록이 없으면 시간표 만들기 화면에서 엑셀 또는 한글 표를 붙여넣거나 빈 표에 직접 입력한 뒤 이름을 붙여 저장한다.
3. 저장 목록이 있으면 메인 화면에서 이번에 사용할 전담 시간표를 고른다.
4. 선택한 시간표의 읽기 전용 미리보기를 확인하고 `선택` 버튼을 누른다.
5. 선택이 끝나면 미리보기 아래 3단계에서 편성할 학년, 강의 시간, 동시 수업 반 수를 정한다.
6. 주간표에서 외부강의를 넣을 수 있는 교시를 개별 선택하거나 드래그로 지정한다.
7. 자동 편성 결과를 전체 시간표와 반별 목록으로 확인한다.

저장 목록을 사용하면 전담 시간표를 브라우저에 이름을 붙여 보관할 수 있다. 저장된 항목을 다시 선택하면 시간표를 복원하고 3단계 편성 조건으로 바로 이동한다.

## 3. 화면

### 3.1 전담 시간표 저장 및 선택

- 클립보드의 HTML 표를 우선 파싱한다.
- HTML 표가 없으면 탭과 줄바꿈으로 구분된 일반 텍스트를 파싱한다.
- 붙여넣은 시간표를 여러 개 추가하거나 삭제할 수 있다.
- 월~금·1~6교시 빈 시간표를 추가해 직접 작성할 수 있다.
- 표의 각 셀을 직접 수정할 수 있다.
- 인식하지 못한 수업 셀은 경고색으로 표시한다.
- 저장된 시간표는 메인 화면의 `저장 목록`에서 선택한다.
- 저장 목록이 비어 있으면 첫 화면에 시간표 만들기 창을 바로 연다.
- 저장 목록에서 고른 시간표는 선택 전 저장 목록 창 안에서 읽기 전용 미리보기로 확인한다.
- 저장 목록 항목을 누르면 먼저 미리보기만 열고, `선택`을 눌렀을 때만 메인 시간표로 불러온다.
- `새로 입력` 화면에서는 붙여넣기 칸·이름·저장 버튼을 순서대로 빨간 테두리로 강조하는 인터랙티브 안내를 제공한다.

### 3.2 강의 조건 및 결과

- 강의 시간 1~4교시 선택
- 동시 수업 가능 반 수 입력
- 학년이 여러 개면 편성할 학년 선택
- 월~금 교시를 개별 클릭·드래그해 외부강의 가능 시간 선택
- 자동 편성 성공 시 전체 표와 반별 배정 목록 표시
- 실패 시 후보가 없는 반 또는 수용 한도 충돌 이유 표시

## 4. 파싱 규칙

- 요일: `월`, `화`, `수`, `목`, `금`
- 교시: `1교시`부터 `6교시`까지, 뒤에 시간 문자열이 있어도 허용
- 기본 반 표기: `4-5 영어`
- 추가 허용 표기: `4-5영어`, `4학년 5반 영어`
- 한 셀에 여러 줄 또는 여러 수업이 있으면 모두 추출
- 동일 반·요일·교시가 여러 표에 나오면 하나의 불가능 시간으로 합치고 사유는 모두 보존

## 5. 자동 편성 규칙

1. 각 반의 전담시간과 겹치지 않는 강의 블록을 생성한다.
2. 후보 블록이 적은 반부터 배정한다.
3. 강의가 차지하는 모든 교시에서 동시 수업 가능 반 수를 넘지 않게 한다.
4. 뒤의 반을 배정할 수 없으면 앞선 배정을 되돌리고 다음 후보를 시도한다.
5. 같은 요일과 시간에 과도하게 몰리지 않도록 현재 사용량이 적은 후보를 우선한다.
6. 같은 입력에는 같은 결과가 나오도록 정렬 기준을 고정한다.

## 6. 기술 구성

- Vite
- React
- TypeScript
- Vitest
- 브라우저 Clipboard API 및 `paste` 이벤트
- 서버와 데이터베이스 없음

## 6.1 디자인 원칙

- 가독성이 높은 시스템 산세리프 글꼴을 본문과 표에 사용한다.
- 흰색과 밝은 회색을 중심으로 하고 파란색 한 가지를 주요 강조색으로 사용한다.
- 장식적인 히어로, 원형 단계 배지, 강한 그림자를 사용하지 않는다.
- 표와 입력 라벨은 15~16px 이상을 기본으로 해 읽기 쉽게 만든다.

## 7. 구현 단계

- [x] 계획 문서 작성
- [x] React/TypeScript 프로젝트 기반 구성
- [x] 표 붙여넣기 및 클립보드 파서
- [x] 학년·반·과목 인식기
- [x] 여러 전담 시간표 통합
- [x] 반별 불가능 시간 미리보기
- [x] 자동 편성 엔진
- [x] 결과 화면 및 실패 사유 표시
- [x] 요일별 교시 개별 선택·드래그 및 편성 제한
- [x] 저장 목록 모달 및 브라우저 localStorage 보관
- [x] 단위 테스트
- [x] 프로덕션 빌드 검증

## 8. 완료 기준

- 전담 시간표 여러 개를 복사·붙여넣기 할 수 있다.
- 각 셀에서 학년·반·과목을 자동으로 찾는다.
- 반별 전담시간을 자동으로 합친다.
- 강의 시간과 동시에 수업하는 반 수만 입력하면 된다.
- 선택하지 않은 요일·교시에는 외부강의를 배정하지 않는다.
- 전담시간과 겹치지 않게 외부 강의를 배정한다.
- 2교시 강의는 `1~2`, `3~4`, `5~6교시`에만 배정한다.
- 편성 불가능 시 원인을 표시한다.
- 가능한 편성안이 여러 개면 최대 100개까지 생성해 이전·다음 및 번호 선택으로 비교할 수 있다.
- `사용방법` 버튼으로 실제 화면을 직접 조작하는 단계별 가이드를 시작할 수 있다.
- 가이드 1단계에서 저장 목록을 열어 전담 시간표를 선택한 뒤 `다음`으로 진행할 수 있다.
- 왼쪽 `저장 목록`에서 전담 시간표를 이름과 함께 저장하고, 저장된 항목을 선택해 3단계부터 바로 시작할 수 있다.
- 저장 목록이 비어 있으면 첫 진입 시 시간표 만들기 화면을 바로 보여준다.
- 저장된 항목을 고르면 저장 목록 창에서 해당 표를 미리본 뒤 `선택`을 눌러 3단계 자동 편성으로 이어진다.
- 목록 항목을 누른 것만으로는 선택하지 않고, 미리보기의 `선택` 버튼을 눌러야 적용한다.
- 새로 입력을 시작하면 한글·엑셀 표 복사와 붙여넣기 방법을 실제 입력 화면 위에서 단계별로 안내한다.
- 현재 보고 있는 편성 결과를 HTML 표와 탭 구분 텍스트로 복사해 엑셀·한글에 붙여넣을 수 있다.
- 프로덕션 빌드와 핵심 단위 테스트가 통과한다.
