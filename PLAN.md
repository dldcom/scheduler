# 외부 강의 시간표 자동 편성기 구현 계획

## 1. 목표

초등학교의 전담 시간표를 엑셀 또는 한글 표에서 복사해 웹앱에 붙여넣으면, 각 셀에서 학년·반·과목을 추출해 반별 배정 불가능 시간을 계산하고 외부 강의를 남은 시간에 자동 배정한다.

첫 버전은 서버 없이 브라우저에서만 동작한다. 사용자가 입력하는 강의 조건은 다음 항목으로 제한한다.

- 각 반이 들어야 하는 강의 시간: 1~4교시
- 한 타임에 동시에 수업하는 반 수(선택한 수만큼 함께 배정하고, 나머지 반은 한 타임에 따로 배정)
- 월~금 요일별 마지막 수업 교시

2교시 강의는 `1~2교시`, `3~4교시`, `5~6교시` 블록에만 배정한다. 3~4교시 강의는 연속 교시로 배정하며, 허용 블록 생성 규칙은 추후 쉽게 변경할 수 있도록 별도 모듈로 둔다.

## 2. 핵심 사용자 흐름

1. 전담 시간표를 엑셀 또는 한글에서 복사한다.
2. 웹앱의 붙여넣기 영역에 붙여넣는다.
   - 복사할 수 없는 경우 빈 시간표를 추가해 셀에 직접 입력한다.
3. 앱이 요일, 교시, 학년, 반, 과목을 자동으로 인식한다.
4. 다른 전담 시간표가 있으면 같은 방식으로 추가한다.
5. 인식 결과를 확인하고 잘못된 셀만 수정한다.
6. 강의 시간과 동시 수업 가능 반 수를 입력한다.
7. 빈 주간표에서 요일별 마지막 교시를 클릭해 수업 범위를 지정한다.
8. 자동 편성 결과를 전체 시간표와 반별 목록으로 확인한다.

## 3. 화면

### 3.1 전담 시간표 입력

- 클립보드의 HTML 표를 우선 파싱한다.
- HTML 표가 없으면 탭과 줄바꿈으로 구분된 일반 텍스트를 파싱한다.
- 붙여넣은 시간표를 여러 개 추가하거나 삭제할 수 있다.
- 월~금·1~6교시 빈 시간표를 추가해 직접 작성할 수 있다.
- 표의 각 셀을 직접 수정할 수 있다.
- 인식하지 못한 수업 셀은 경고색으로 표시한다.

### 3.2 인식 결과

- 발견된 학년과 반 목록을 표시한다.
- 반별 전담시간을 요일·교시 표로 합쳐 보여준다.
- 과목명을 보존해 해당 시간이 불가능한 이유를 확인할 수 있게 한다.

### 3.3 강의 조건 및 결과

- 강의 시간 1~4교시 선택
- 동시 수업 가능 반 수 입력
- 학년이 여러 개면 편성할 학년 선택
- 월~금 각 요일의 마지막 교시를 클릭해 실제 수업 범위 선택
- 자동 편성 성공 시 전체 표와 반별 배정 목록 표시
- 실패 시 후보가 없는 반 또는 수용 한도 충돌 이유 표시

## 4. 파싱 규칙

- 요일: `월`, `화`, `수`, `목`, `금`
- 교시: `1교시`부터 `6교시`까지, 뒤에 시간 문자열이 있어도 허용
- 기본 반 표기: `4-5 영어`
- 추가 허용 표기: `4-5영어`, `4학년 5반 영어`
- 한 셀에 여러 줄 또는 여러 수업이 있으면 모두 추출
- 동일 반·요일·교시가 여러 표에 나오면 하나의 불가능 시간으로 합치고 사유는 모두 보존

## 5. 자동 편성 규칙

1. 각 반의 전담시간과 겹치지 않는 강의 블록을 생성한다.
2. 후보 블록이 적은 반부터 배정한다.
3. 강의가 차지하는 모든 교시에서 동시 수업 가능 반 수를 넘지 않게 한다.
4. 뒤의 반을 배정할 수 없으면 앞선 배정을 되돌리고 다음 후보를 시도한다.
5. 같은 요일과 시간에 과도하게 몰리지 않도록 현재 사용량이 적은 후보를 우선한다.
6. 같은 입력에는 같은 결과가 나오도록 정렬 기준을 고정한다.

## 6. 기술 구성

- Vite
- React
- TypeScript
- Vitest
- 브라우저 Clipboard API 및 `paste` 이벤트
- 서버와 데이터베이스 없음

## 6.1 디자인 원칙

- 가독성이 높은 시스템 산세리프 글꼴을 본문과 표에 사용한다.
- 흰색과 밝은 회색을 중심으로 하고 파란색 한 가지를 주요 강조색으로 사용한다.
- 장식적인 히어로, 원형 단계 배지, 강한 그림자를 사용하지 않는다.
- 표와 입력 라벨은 15~16px 이상을 기본으로 해 읽기 쉽게 만든다.

## 7. 구현 단계

- [x] 계획 문서 작성
- [x] React/TypeScript 프로젝트 기반 구성
- [x] 표 붙여넣기 및 클립보드 파서
- [x] 학년·반·과목 인식기
- [x] 여러 전담 시간표 통합
- [x] 반별 불가능 시간 미리보기
- [x] 자동 편성 엔진
- [x] 결과 화면 및 실패 사유 표시
- [x] 요일별 마지막 수업 교시 선택 및 편성 제한
- [x] 단위 테스트
- [x] 프로덕션 빌드 검증

## 8. 완료 기준

- 전담 시간표 여러 개를 복사·붙여넣기 할 수 있다.
- 각 셀에서 학년·반·과목을 자동으로 찾는다.
- 반별 전담시간을 자동으로 합친다.
- 강의 시간과 동시에 수업하는 반 수만 입력하면 된다.
- 요일별 마지막 수업 교시 이후에는 외부강의를 배정하지 않는다.
- 전담시간과 겹치지 않게 외부 강의를 배정한다.
- 2교시 강의는 `1~2`, `3~4`, `5~6교시`에만 배정한다.
- 편성 불가능 시 원인을 표시한다.
- 가능한 편성안이 여러 개면 최대 100개까지 생성해 이전·다음 및 번호 선택으로 비교할 수 있다.
- `사용방법` 버튼으로 실제 화면을 직접 조작하는 단계별 가이드를 시작할 수 있다.
- 가이드 1단계에서 여러 시간표를 모두 붙여넣은 뒤 `다음`을 누르거나, `직접 입력하겠습니다`로 빈 시간표를 만들 수 있다.
- 현재 보고 있는 편성 결과를 HTML 표와 탭 구분 텍스트로 복사해 엑셀·한글에 붙여넣을 수 있다.
- 프로덕션 빌드와 핵심 단위 테스트가 통과한다.
